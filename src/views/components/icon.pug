mixin icon-mono(name)
    svg(class='icon icon-' + name)&attributes(attributes)
        use(xlink:href='img/icons/sprites/sprite-mono.svg#'+ name)

mixin icon-multi(name)
    svg(class='icon icon-' + name)&attributes(attributes)
        use(xlink:href='img/icons/sprites/sprite-multi.svg#' + name)